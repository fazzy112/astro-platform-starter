---
import Layout from '../layouts/Layout.astro';
import ResumeFormModal from '../components/ResumeFormModal.astro';
---

<Layout title="ResumeAI - Create Your AI-Powered Resume Instantly">
  <!-- Modals -->
  <ResumeFormModal />

  <!-- Hero Section -->
  <section class="relative overflow-hidden pt-16 pb-24 px-4 bg-gradient-to-br from-indigo-500 via-purple-600 to-pink-500 text-white">
    <div class="max-w-7xl mx-auto grid lg:grid-cols-12 gap-8 items-center">
      <div class="lg:col-span-6">
        <h1 class="text-4xl font-extrabold sm:text-5xl md:text-6xl">
          <span class="block">Create Your</span>
          <span class="block text-transparent bg-clip-text bg-gradient-to-r from-indigo-400 to-purple-500">AI-Powered Resume</span>
          <span class="block">Instantly!</span>
        </h1>
        <p class="mt-5 text-lg text-white opacity-90">
          Get a professionally crafted, ATS-optimized resume in minutes. Our AI analyzes your experience and tailors it to your dream job.
        </p>
        <div class="mt-8 flex flex-col sm:flex-row gap-4">
          <!-- ✅ Final working button -->
          <button id="open-resume-form" class="bg-blue-600 text-white px-6 py-3 rounded-lg shadow hover:bg-blue-700 transition">
            Build Resume from Form
          </button>
          <button class="btn btn-secondary">Upload Existing Resume</button>
        </div>
      </div>

      <div class="lg:col-span-6 flex justify-center">
        <img class="rounded-lg shadow-lg" src="/images/resume-preview.svg" alt="Resume Preview" />
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section id="how-it-works" class="py-16 bg-white text-gray-800">
    <div class="max-w-7xl mx-auto px-4">
      <h2 class="text-3xl font-extrabold text-center">How It Works</h2>
      <p class="mt-4 text-xl text-center text-gray-500">Create your professional resume in just four simple steps</p>
      <div class="mt-12 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <!-- Step Cards... -->
      </div>
    </div>
  </section>

  <!-- Benefits Section -->
  <section id="benefits" class="py-16 bg-gray-50 text-gray-900">
    <!-- same as yours -->
  </section>

  <!-- Testimonials Section -->
  <section id="testimonials" class="py-16 bg-white text-gray-800">
    <!-- same as yours -->
  </section>

  <!-- Waitlist CTA -->
  <section id="waitlist" class="py-16 bg-gradient-to-r from-indigo-600 to-purple-600 text-white text-center">
    <!-- same as yours -->
  </section>
</Layout>
